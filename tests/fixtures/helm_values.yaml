# Helm values for webapp
webapp:
  replicas: 3
  image:
    repository: ghcr.io/myorg/webapp
    tag: v1.0.0
  resources:
    limits:
      cpu: 500m
      memory: 256Mi

# Backend service
backend:
  image:
    repository: ghcr.io/myorg/backend
    tag: v2.3.1
  port: 8080
